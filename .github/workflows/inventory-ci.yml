name: Inventory API CI

on:
  pull_request:
    paths:
      - 'inventory-api/**'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig*.json'
      - '.eslintrc*'
      - 'jest.base.config.ts'
      - 'tools/**'
  push:
    branches:
      - main
    paths:
      - 'inventory-api/**'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig*.json'
      - '.eslintrc*'
      - 'jest.base.config.ts'
      - 'tools/**'

jobs:
  pact-flow:
    # Wait for pact-flow workflow to complete successfully
    # This ensures contracts are verified and mocks are generated
    uses: ./.github/workflows/pact-flow.yml

  inventory-tests:
    needs: pact-flow
    # Unit and integration tests for inventory-api
    uses: ./.github/workflows/_node-ci.yml
    with:
      workspace: 'inventory-api'
      run_coverage: true
    secrets: inherit
