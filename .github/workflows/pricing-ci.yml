name: Pricing API CI

on:
  pull_request:
    paths:
      - 'pricing-api/**'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig*.json'
      - '.eslintrc*'
      - 'jest.base.config.ts'
      - 'tools/**'
  push:
    branches:
      - main
    paths:
      - 'pricing-api/**'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig*.json'
      - '.eslintrc*'
      - 'jest.base.config.ts'
      - 'tools/**'

jobs:
  pact-flow:
    # Wait for pact-flow workflow to complete successfully
    # This ensures contracts are verified and mocks are generated
    uses: ./.github/workflows/pact-flow.yml

  pricing-tests:
    needs: pact-flow
    # Unit and integration tests for pricing-api
    uses: ./.github/workflows/_node-ci.yml
    with:
      workspace: 'pricing-api'
      run_coverage: true
    secrets: inherit
